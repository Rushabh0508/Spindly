<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Spendly"
        android:supportsRtl="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Spendly">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Shown the instant a Monzo notification is caught. Uses a dialog-style
          theme so it renders as a floating card over whatever app is on screen,
          and stays on top even if Spendly itself isn't open.
        -->
        <activity
            android:name=".ui.CategorizeActivity"
            android:exported="false"
            android:theme="@style/Theme.Spendly.CategorizeDialog"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true" />

        <service
            android:name=".BankNotificationListener"
            android:exported="false"
            android:label="Spendly notification reader"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
